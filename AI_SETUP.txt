PRIME AI — GEMINI 404 FIX

Исправлено:
- модель по умолчанию: gemini-2.5-flash (стабильная multimodal модель);
- API key передаётся официальным заголовком x-goog-api-key;
- при ошибке приложение показывает подробности Google.

Запуск:
1. Закрой старый PRIME и окно Python.
2. В этой папке открой CMD.
3. Выполни:
   set GEMINI_API_KEY=ТВОЙ_КЛЮЧ
4. Затем:
   START.bat
5. Открой http://127.0.0.1:8765
6. Face -> фото -> Анализировать.

Если используешь setx, после него открой НОВОЕ окно CMD.
